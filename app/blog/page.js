import BlogIndexContent from '@/components/BlogIndexContent';

export const metadata = {
  title: 'Blog — GapAnchor',
  description: 'Insights, resources, and career advice for WMS and Supply Chain professionals. Stay updated with the latest trends and stories.',
};

export default function BlogPage() {
  return <BlogIndexContent />;
}
