'use client';

export default function ConnectionLines() {
  // Completely removed the complex SVG connection lines to eliminate 
  // the dark bounding box clipping artifacts entirely.
  return null;
}
